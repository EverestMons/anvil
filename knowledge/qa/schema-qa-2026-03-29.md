# Anvil Schema QA Report
**Date:** 2026-03-29
**Agent:** Anvil QA Analyst
**Blueprint:** `anvil/knowledge/architecture/schema-blueprint-2026-03-29.md`
**Dev Log:** `anvil/knowledge/development/schema-implementation-2026-03-29.md`

---

## Verification Areas

### 1. Table Completeness — PASS
Ran `SELECT name FROM sqlite_master WHERE type='table' ORDER BY name` against live `anvil.db`.

**Expected (10):** chunk_dependencies, chunk_fingerprints, chunk_similarities, chunk_symbol_bindings, code_chunks, cycle_reports, git_changes, health_scores, projects, test_results

**Actual (10):** Exact match. (`sqlite_sequence` excluded — internal SQLite table from AUTOINCREMENT.)

### 2. Column Accuracy — PASS (all 10 tables)

| Table | Columns | Types/Constraints | Status |
|---|---|---|---|
| projects | id, name, path, last_scanned, created_at | name UNIQUE, NOT NULL on name/path/created_at, default datetime('now') | PASS |
| code_chunks | id, project_id, file_path, chunk_type, name, content, content_hash, start_line, end_line, parent_chunk_id, created_at, updated_at, cycle_id | CHECK on chunk_type, parent_chunk_id nullable, cycle_id nullable | PASS |
| chunk_fingerprints | id, chunk_id, content_hash, minhash_signature, shingle_count, cycle_id | minhash_signature BLOB nullable, shingle_count nullable | PASS |
| chunk_symbol_bindings | id, chunk_id, symbol_name, binding_type, target_chunk_id, created_at | CHECK on binding_type, target_chunk_id nullable | PASS |
| chunk_dependencies | id, source_chunk_id, target_chunk_id, dependency_type, scope, created_at | CHECK on dependency_type and scope, both FKs NOT NULL | PASS |
| chunk_similarities | id, chunk_a_id, chunk_b_id, similarity_score, cycle_id, created_at | similarity_score REAL NOT NULL, both FKs NOT NULL | PASS |
| git_changes | id, project_id, file_path, commit_hash, commit_date, commit_message, author, created_at | commit_message/author nullable | PASS |
| test_results | id, project_id, run_date, total_tests, passed, failed, skipped, failed_test_names, cycle_id, created_at | failed_test_names/cycle_id nullable | PASS |
| health_scores | id, chunk_id, volatility_score, coverage_score, complexity_score, coupling_score, staleness_score, composite_score, cycle_id, created_at | All scores REAL NOT NULL DEFAULT 0.0 | PASS |
| cycle_reports | id, project_id, cycle_number, started_at, completed_at, files_scanned, chunks_extracted, chunks_scored, findings_count, report_path, created_at | completed_at/report_path nullable, counters default 0 | PASS |

### 3. Foreign Key Integrity — PASS (all 13 FKs)

Verified with `PRAGMA foreign_key_list()` per table. All 13 FKs match the blueprint Foreign Key Map:

| Table | FK Column | References | On Delete | Status |
|---|---|---|---|---|
| code_chunks | project_id | projects(id) | CASCADE | PASS |
| code_chunks | parent_chunk_id | code_chunks(id) | SET NULL | PASS |
| chunk_fingerprints | chunk_id | code_chunks(id) | CASCADE | PASS |
| chunk_symbol_bindings | chunk_id | code_chunks(id) | CASCADE | PASS |
| chunk_symbol_bindings | target_chunk_id | code_chunks(id) | SET NULL | PASS |
| chunk_dependencies | source_chunk_id | code_chunks(id) | CASCADE | PASS |
| chunk_dependencies | target_chunk_id | code_chunks(id) | CASCADE | PASS |
| chunk_similarities | chunk_a_id | code_chunks(id) | CASCADE | PASS |
| chunk_similarities | chunk_b_id | code_chunks(id) | CASCADE | PASS |
| git_changes | project_id | projects(id) | CASCADE | PASS |
| test_results | project_id | projects(id) | CASCADE | PASS |
| health_scores | chunk_id | code_chunks(id) | CASCADE | PASS |
| cycle_reports | project_id | projects(id) | CASCADE | PASS |

**FK enforcement verified:** Inserting code_chunks with project_id=9999 raises IntegrityError.

### 4. Index Coverage — PASS (21/21 indexes)

Verified with `PRAGMA index_list()` per table. All 21 indexes from blueprint exist:

- code_chunks: 4 indexes (project_file composite, chunk_type, parent, content_hash)
- chunk_fingerprints: 2 indexes (chunk, cycle)
- chunk_symbol_bindings: 4 indexes (chunk, symbol, target, type)
- chunk_dependencies: 2 indexes (source, target)
- chunk_similarities: 3 indexes (a, b, cycle)
- git_changes: 2 indexes (project_file composite, commit_hash)
- test_results: 1 index (project)
- health_scores: 1 index (chunk_cycle composite)
- cycle_reports: 2 indexes (project, project_cycle UNIQUE)

### 5. CRUD Verification — PASS (34/34 tests)

```
34 passed in 0.03s
```

No failures. No skips. Coverage areas: schema creation, CRUD for all 10 tables, FK enforcement (9 tests), CHECK constraints (4 tests), unique constraints (2 tests), self-referential FK, direction-based dependency queries.

### 6. Blueprint Compliance — PASS (all 7 checks)

Executed every check from the SA blueprint "How to verify" section:
1. Table completeness — 10/10 PASS
2. Column accuracy — all tables PASS
3. FK integrity — 13/13 PASS, enforcement verified
4. Index coverage — 21/21 PASS
5. CHECK constraints — 4/4 PASS (chunk_type, binding_type, dependency_type, scope)
6. WAL mode = `wal`, foreign_keys = `1` — PASS
7. Forge pattern compliance — see below

### 7. Forge Pattern Compliance — PASS

- `init_db(conn)` exists, takes `conn` as sole parameter — PASS
- `_row_to_dict(cursor, row)` helper exists — PASS
- All 16 CRUD functions take `conn` as first argument — PASS

---

## Summary

**Overall Result: PASS**

All 7 verification areas pass. Schema implementation matches the SA blueprint exactly — 10 tables, 13 foreign keys, 21 indexes, 4 CHECK constraints, WAL mode, Forge-pattern CRUD. No fixes required.

---

## Output Receipt
**Agent:** Anvil QA Analyst
**Step:** Step 3
**Status:** Complete

### What Was Done
Verified the Anvil schema implementation against the SA blueprint across 7 verification areas. All checks pass — tables, columns, FKs, indexes, CHECK constraints, CRUD tests, and Forge pattern compliance.

### Files Deposited
- `anvil/knowledge/qa/schema-qa-2026-03-29.md` — schema QA report

### Files Created or Modified (Code)
- None

### Decisions Made
- All 7 areas PASS — no fixes needed

### Flags for CEO
- None

### Flags for Next Step
- None — schema is verified and ready for pipeline implementation
