# Anvil — Phase 0 Scaffold
**Date:** 2026-03-29 | **Tier:** Small | **Execution:** Step 1 (DEV) → Step 2 (DEV) → Step 3 (QA)

## How to Run This Plan

Read the plan at `anvil/knowledge/decisions/executable-scaffold-2026-03-29.md`. Execute Step 1. After completing Step 1, stop and wait for my confirmation before proceeding to Step 2.

---
---

## STEP 1 — ANVIL DEVELOPER (git + COMPANY.md + CLAUDE.md + PROJECT_STATUS.md)

---

> Skip specialist file and glossary reads — this is a scaffolding task. **Git init:** Run `cd /Users/marklehn/Desktop/GitHub/anvil && git init && git add PROJECT_BRIEF.md knowledge/ agents/ src/ tests/ && git commit -m "init: project structure + PROJECT_BRIEF"`. **COMPANY.md update:** Read `/Users/marklehn/Desktop/GitHub/COMPANY.md`. In the Active Projects table, add a row: `| anvil | Structural codebase intelligence — analyzes code structure, testing, volatility, patterns across projects | Active |`. Update the forge row description to: `| forge | Prompt Forge — ingests operational artifacts across all projects, extracts prompt patterns, scores maturity, outputs governance updates | Active |`. Both forge and anvil are under the "Forge" umbrella: Prompt Forge (governance improvement) and Code Forge/Anvil (code intelligence). Use `with open()` for all file writes — no heredocs. Commit: `git -C /Users/marklehn/Desktop/GitHub add COMPANY.md && git -C /Users/marklehn/Desktop/GitHub/eluvian-governance commit -m "docs: add anvil to active projects, clarify forge description"` — if that repo path doesn't work, just commit in the root GitHub directory. **CLAUDE.md:** Create `anvil/CLAUDE.md` with these rules: (1) All file writes use Python `with open(..., "w") as f: f.write(content)` — never bash heredocs (heredocs corrupt JSON files). (2) Run tests with `cd anvil && python3 -m pytest tests/ -v` before every commit. (3) Commit messages follow conventional commits: `feat:`, `fix:`, `docs:`, `chore:`, `test:`. (4) Never modify files outside the anvil directory without explicit CEO approval. (5) Read your specialist file at `anvil/agents/` before starting any task. (6) Read the domain glossary at `anvil/knowledge/research/domain-glossary.md` for Anvil-specific terminology. (7) Python 3.x, SQLite, no external API dependencies — Claude Code is the intelligence layer. (8) All knowledge deposits use `with open()` with absolute paths.
> **PROJECT_STATUS.md:** Create `anvil/PROJECT_STATUS.md` with: Status: Scaffolding. Phase 0 in progress. No code yet. Roadmap deposited at `knowledge/decisions/roadmap-anvil-build-2026-03-29.md`. CEO decisions locked (Claude Code function execution model, run tests directly first, separate DBs from Forge). Next: agent specialist files (Step 2), then Phase 1 (Schema). **requirements.txt:** Create `anvil/requirements.txt` with: `datasketch>=1.6.0` (MinHash), `pytest>=7.0.0` (testing). No other external deps for Phase 1 — Python AST is stdlib. Commit all new files: `git -C /Users/marklehn/Desktop/GitHub/anvil add -A && git -C /Users/marklehn/Desktop/GitHub/anvil commit -m "docs: CLAUDE.md + PROJECT_STATUS.md + requirements.txt"`.

---
---

## STEP 2 — ANVIL DEVELOPER (agent files + glossary + feedback log)

---

> Before starting, verify Step 1 committed by running `git -C /Users/marklehn/Desktop/GitHub/anvil log --oneline -3`. Skip specialist file and glossary reads — this is a scaffolding task. Read the SPECIALIST_TEMPLATE at `/Users/marklehn/Desktop/GitHub/SPECIALIST_TEMPLATE.md` and use it as the structural template for all three agent files. Read `invoice-pulse/agents/INVOICE_DEVELOPER.md` (first 80 lines) as a quality reference for tone and specificity. Read the Anvil PROJECT_BRIEF at `anvil/PROJECT_BRIEF.md` for project context to embed in each agent. **Create 3 specialist files using `with open()` for each:**
> **(1) `anvil/agents/ANVIL_SYSTEMS_ANALYST.md`** — Role: Anvil Systems Analyst. Department: Systems Architecture. Project: anvil. Domain Focus: SQLite schema design for code analysis (code_chunks, chunk_fingerprints, chunk_symbol_bindings, chunk_dependencies, chunk_similarities, git_changes, test_results, health_scores, cycle_reports), pipeline architecture (SCAN → EXTRACT → SCORE → LAB), cross-project structural pattern analysis. Key Sources: Anvil PROJECT_BRIEF (data model section), study chunk diagnostics (`study/knowledge/research/chunk-metadata-diagnostic-2026-03-22.md`, `study/knowledge/research/chunk-relationships-diagnostic-2026-03-22.md`), Forge's scanner/extractor patterns (`forge/src/scanner.py`, `forge/src/extractor.py`). Core Responsibilities: design schema migrations, blueprint pipeline stage interfaces, define chunk granularity rules per language, specify scoring formulas, design Lab output formats. Decision Authority: schema design within PROJECT_BRIEF constraints (Specialist), schema changes that deviate from brief (Escalate to CEO), scoring weight changes (Escalate to CEO). Peer Consultation: Anvil Developer (implementation feasibility), Anvil QA (testability of blueprints). Project-Specific Guardrails: do not design for languages beyond Phase 1 Python without CEO approval, schema must match PROJECT_BRIEF data model unless deviation is justified and approved, all blueprints must include "How to verify this was implemented correctly" section.
> **(2) `anvil/agents/ANVIL_DEVELOPER.md`** — Role: Anvil Developer. Department: Development. Project: anvil. Domain Focus: Python implementation of the SCAN → EXTRACT → SCORE → LAB pipeline, Python AST parsing for code chunk extraction, SQLite operations (schema creation, CRUD, migrations), git CLI integration for history ingestion, MinHash computation via datasketch, content hashing (SHA-256) for change detection. Key Sources: Python `ast` module docs, `datasketch` library docs, Forge's `scanner.py` and `db.py` patterns, invoice-pulse codebase as the Phase 1 analysis target. Core Responsibilities: implement pipeline stages per SA blueprints, write and maintain test suite, build Python AST parser for function/class/method extraction, implement MinHash similarity detection with persistent storage, build git log parser for volatility tracking. Project-Specific Procedure: run `cd anvil && python3 -m pytest tests/ -v` before every commit. All file writes use `with open()` — never heredocs. Decision Authority: implementation approach within blueprint (Specialist), deviating from blueprint (Escalate to SA), adding new tables or columns (Escalate to SA then CEO). Peer Consultation: Anvil SA (schema questions), Anvil QA (test strategy). Project-Specific Guardrails: do not modify invoice-pulse source files — Anvil is read-only on target projects, do not use external APIs — Claude Code is the intelligence layer, do not skip tests for "small" changes.
> **(3) `anvil/agents/ANVIL_QA_ANALYST.md`** — Role: Anvil QA Analyst. Department: Security & Testing. Project: anvil. Domain Focus: pipeline output validation (are extracted chunks accurate? do health scores reflect real code quality?), schema verification (PRAGMA checks against live DB), test coverage analysis (does the test suite cover all pipeline stages?), cross-validation against known project facts (does Anvil's output match what specialist files say about invoice-pulse?). Key Sources: Anvil test suite, invoice-pulse specialist files (for cross-validation data — known gate count, table count, test count), Forge QA patterns. Core Responsibilities: verify each pipeline stage produces correct output, validate schema against PROJECT_BRIEF data model, cross-check Anvil findings against known project state, verify MinHash similarity detection accuracy, verify git history ingestion completeness. Project-Specific Procedure: QA reports use numbered verification areas with PASS/FAIL per area. Always run `PRAGMA table_info()` against live DB after schema changes. Project-Specific Guardrails: do not sign off on a pipeline stage that has zero test coverage, do not accept "verify it works" as a test specification — require explicit expected values. Decision Authority: PASS/FAIL determination (Specialist), halting a pipeline stage for critical issues (Specialist), waiving a test requirement (Escalate to CEO). Peer Consultation: Anvil Developer (test infrastructure questions), Anvil SA (schema verification questions). **All three files must pass the SPECIALIST_TEMPLATE completeness checklist (11 sections).** Fill every section — no placeholders.
> **Domain glossary:** Create `anvil/knowledge/research/domain-glossary.md` with initial terms: code chunk (atomic unit of code — function, class, method, module, config block, test case), symbol binding (typed relationship between a chunk and a symbol — defines/imports/calls/tests/documents), chunk dependency (directed edge between chunks — import chain, call graph, inheritance), health score (composite per-chunk score from volatility + coverage + complexity + coupling + staleness), cycle (one complete SCAN → EXTRACT → SCORE → LAB run), volatility (change frequency from git history), coverage (test bindings for a chunk), coupling (inbound + outbound dependency count), staleness (chunk last modified vs its dependencies' last modified), MinHash signature (locality-sensitive hash for near-duplicate detection), content hash (SHA-256 of file content for change detection between cycles). **Feedback log:** Create `anvil/knowledge/research/agent-prompt-feedback.md` with header "# Anvil — Agent Prompt Feedback Log" and an empty "## Patterns Identified" section at the bottom. Commit: `git -C /Users/marklehn/Desktop/GitHub/anvil add -A && git -C /Users/marklehn/Desktop/GitHub/anvil commit -m "docs: agent specialist files + domain glossary + feedback log"`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.

---
---

## STEP 3 — ANVIL QA ANALYST (completeness verification)

---

> Before starting, verify Step 2 committed by running `git -C /Users/marklehn/Desktop/GitHub/anvil log --oneline -3`. Read the SPECIALIST_TEMPLATE at `/Users/marklehn/Desktop/GitHub/SPECIALIST_TEMPLATE.md` — specifically the Completeness Checklist at the bottom (11 sections, all must be present and filled). Read all three agent files: `anvil/agents/ANVIL_SYSTEMS_ANALYST.md`, `anvil/agents/ANVIL_DEVELOPER.md`, `anvil/agents/ANVIL_QA_ANALYST.md`. **Verify each file against the completeness checklist:** (1) Header with all required fields, (2) Role Summary — project-specific paragraph, (3) Project Context — Domain Focus + Key Sources + Project-Specific Context all filled, (4) Core Responsibilities — 3-6 bullets, (5) Operating Procedure — inheritance + project-specific, (6) Output Format — inheritance + output location path, (7) Decision Authority — inheritance + table with 2+ rows, (8) Peer Consultation — table with 1+ entry, (9) Quality Standards — inheritance + project-specific notes, (10) Guardrails — inheritance + project-specific, (11) Knowledge Base Index — table present. Also verify: CLAUDE.md exists and contains all 8 rules, PROJECT_STATUS.md exists, domain-glossary.md has all terms, feedback log has Patterns Identified section, requirements.txt lists datasketch and pytest, COMPANY.md has anvil row and updated forge description. Report per-file PASS/FAIL with specific gaps if any. Fix any gaps found (agent has write authority for QA fixes on documentation). Deposit QA report to `anvil/knowledge/qa/scaffold-qa-2026-03-29.md` using `with open("/Users/marklehn/Desktop/GitHub/anvil/knowledge/qa/scaffold-qa-2026-03-29.md", "w") as f: f.write(content)`. **Final:** Update `anvil/PROJECT_STATUS.md` — add completed milestone: "Phase 0 Scaffold complete — git init, COMPANY.md updated, CLAUDE.md, 3 agent specialist files, domain glossary, feedback log, requirements.txt." Move this plan to Done: `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/anvil/knowledge/decisions/executable-scaffold-2026-03-29.md", "/Users/marklehn/Desktop/GitHub/anvil/knowledge/decisions/Done/executable-scaffold-2026-03-29.md")`. Commit: `git -C /Users/marklehn/Desktop/GitHub/anvil add -A && git -C /Users/marklehn/Desktop/GitHub/anvil commit -m "chore: scaffold QA + status update + move plan to Done"`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.
