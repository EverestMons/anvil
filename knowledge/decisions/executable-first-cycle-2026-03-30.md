# Anvil — Phase 6 First Cycle Validation
**Date:** 2026-03-30 | **Tier:** Medium | **Execution:** Step 1 (DEV) → Step 2 (QA) → Step 3 (QA)

## How to Run This Plan

Read the plan at `anvil/knowledge/decisions/executable-first-cycle-2026-03-30.md`. Execute Step 1. After completing Step 1, stop and wait for my confirmation before proceeding to Step 2.

---
---

## STEP 1 — ANVIL DEVELOPER (full pipeline end-to-end run + wiring)

---

> You are the Anvil Developer. Read your specialist file at `anvil/agents/ANVIL_DEVELOPER.md` and the domain glossary at `anvil/knowledge/research/domain-glossary.md` first. Read `anvil/src/config.py`, `anvil/src/db.py`, `anvil/src/scanner.py`, `anvil/src/extractor.py`, `anvil/src/scorer.py`, `anvil/src/lab.py` — scan the function signatures (don't read line-by-line, just the `def` lines and docstrings). **Task:** Create a cycle runner that wires the full pipeline together and validates the existing Cycle 1 data. Two deliverables: **(A) `anvil/src/cycle.py`** — the pipeline orchestrator. Must implement: (1) `run_cycle(conn, project_name)` — runs SCAN → EXTRACT → SCORE → LAB in sequence. Assigns a new cycle_id (increment from max existing cycle_reports.cycle_number + 1, or 1 if none). Calls `scanner.scan_project(conn, project_name)`, then `extractor.extract_project(conn, project_name, cycle_id)`, then `scorer.score_project(conn, project_name, cycle_id)`, then `lab.run_lab(conn, project_name, cycle_id)`. Returns a combined summary dict with per-stage results and total elapsed time. Handles errors per stage: if scanner fails, abort. If extractor fails, log warning and skip scoring/lab. If scorer fails, still run lab with whatever data exists. (2) `get_cycle_summary(conn, project_name, cycle_number)` — reads cycle_reports row + health_scores stats for a given cycle, returns a summary for the CEO. (3) `compare_cycles(conn, project_name, cycle_a, cycle_b)` — compares two cycle runs: new/removed chunks, score changes (improved/degraded/unchanged), new/resolved findings. This is how Anvil shows progress over time. **(B) Validate Cycle 1 data integrity** — since the pipeline stages ran individually during Phases 2-5, verify the data is consistent as a complete cycle: run `get_cycle_summary(conn, "invoice-pulse", 1)` against the existing DB and confirm all numbers match the QA reports (939 files, 3247 chunks, 3247 scores, 1212 findings, 292 constraints). If cycle_reports row is missing or incomplete, backfill it from the actual data. **Tests:** Create `anvil/tests/test_cycle.py`: (1) run_cycle end-to-end with a small mock project (3-4 .py files), verify all stages complete and cycle_report created, (2) get_cycle_summary returns correct structure, (3) compare_cycles with two mock cycles showing a new chunk and a score change, (4) error handling — mock a scanner failure, verify clean abort. Run full suite: `cd /Users/marklehn/Desktop/GitHub/anvil && python3 -m pytest tests/ -v`. Report total test count across all test files. Commit: `git -C /Users/marklehn/Desktop/GitHub/anvil add -A && git -C /Users/marklehn/Desktop/GitHub/anvil commit -m "feat: cycle runner — pipeline orchestrator, cycle comparison, data validation"`. Deposit dev log to `anvil/knowledge/development/cycle-runner-implementation-2026-03-30.md` using `with open()`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.

---
---

## STEP 2 — ANVIL QA ANALYST (cross-validation against known project facts)

---

> You are the Anvil QA Analyst. Read your specialist file at `anvil/agents/ANVIL_QA_ANALYST.md` and the domain glossary first. Before starting, read the dev log at `anvil/knowledge/development/cycle-runner-implementation-2026-03-30.md` and check its Output Receipt status. If not Complete, stop and report. **Task:** Cross-validate Anvil's Cycle 1 output against known facts about invoice-pulse. This is the proof that Anvil's structural intelligence is correct. Read the invoice-pulse developer specialist file at `invoice-pulse/agents/INVOICE_DEVELOPER.md` for known project facts (test count, table count, engine count, route count, etc.). Then validate against Anvil's DB. **10 verification areas:**
> **(1) Test count accuracy** — the specialist file says 944+ tests across 65 test files. Query Anvil: `SELECT COUNT(*) FROM code_chunks WHERE project_id = 1 AND chunk_type = 'test_case'`. Also count test files: `SELECT COUNT(DISTINCT file_path) FROM code_chunks WHERE project_id = 1 AND file_path LIKE '%test_%' AND chunk_type = 'module'`. Compare against specialist's claimed counts. Note: Anvil counts may differ slightly (specialist says 944+, Anvil extracted 1720 test_case chunks — this may count individual test methods vs test functions, which is acceptable if explainable). PASS/FAIL with explanation. **(2) Engine module count** — specialist file mentions engines/ with 27 modules. Query: `SELECT COUNT(DISTINCT file_path) FROM code_chunks WHERE project_id = 1 AND file_path LIKE '%engines/%' AND chunk_type = 'module'`. Compare. PASS/FAIL. **(3) Blueprint/route count** — specialist mentions 20 blueprint files and ~180+ routes. Query: `SELECT COUNT(DISTINCT file_path) FROM code_chunks WHERE project_id = 1 AND file_path LIKE '%web/%' AND chunk_type = 'module'`. PASS/FAIL. **(4) Validator gate functions** — specialist mentions 11 gates (10 cached + 1 non-cached). Query: `SELECT name FROM code_chunks WHERE project_id = 1 AND file_path LIKE '%validator.py' AND (name LIKE 'gate_%' OR name LIKE '_gate_%')`. Count and list them. PASS/FAIL. **(5) Confidence engine integrity** — the confidence state machine is FROZEN per specialist. Query: `SELECT name FROM code_chunks WHERE project_id = 1 AND file_path LIKE '%confidence.py'`. Verify key functions exist (transition_state, record_evidence, resolve_evidence). Verify confidence.py has a staleness alert (it should — dependencies have changed but the frozen module hasn't). PASS/FAIL. **(6) Known parallel implementations** — the Planner knows about cached/non-cached parallel paths in validator.py. Query: `SELECT name FROM code_chunks WHERE project_id = 1 AND file_path LIKE '%validator.py' AND name LIKE '%cached%'`. Does Anvil detect these as similar chunks in chunk_similarities? Query: `SELECT cs.similarity_score, a.name, b.name FROM chunk_similarities cs JOIN code_chunks a ON cs.chunk_a_id = a.id JOIN code_chunks b ON cs.chunk_b_id = b.id WHERE a.file_path LIKE '%validator.py' AND b.file_path LIKE '%validator.py' AND cs.similarity_score > 0.5`. PASS/FAIL. **(7) Database table count** — specialist says 67+ tables. Query Anvil for SQLAlchemy model chunks or — since invoice-pulse uses raw SQLite, not ORM — check if `create_contract_tables` and `_create_tables` are extracted as function chunks and their coupling reflects the ~67 tables they create. PASS/FAIL. **(8) Clone detection plausibility** — Anvil found 381 clone pairs. Review the top-5 1.0-similarity pairs. Are they genuinely identical code? Read the actual chunks: `SELECT a.content, b.content FROM chunk_similarities cs JOIN code_chunks a ON cs.chunk_a_id = a.id JOIN code_chunks b ON cs.chunk_b_id = b.id WHERE cs.similarity_score = 1.0 LIMIT 2`. Verify the content is actually identical (or near-identical). PASS/FAIL. **(9) Coverage gap plausibility** — Anvil flagged 117 coverage gaps. The top ones are gate functions in validator.py. Cross-check: are there actually no test functions that reference these gates? Query: `SELECT COUNT(*) FROM chunk_symbol_bindings WHERE binding_type = 'tests' AND target_chunk_id IN (SELECT id FROM code_chunks WHERE name = 'gate_9_accessorials')`. If 0, the coverage gap is genuine. Check 3 more from the top list. PASS/FAIL. **(10) Full test suite** — run `cd /Users/marklehn/Desktop/GitHub/anvil && python3 -m pytest tests/ -v`. Report total test count across ALL test files and confirm 0 failures. PASS/FAIL.
> Deposit QA report to `anvil/knowledge/qa/first-cycle-validation-2026-03-30.md` using `with open("/Users/marklehn/Desktop/GitHub/anvil/knowledge/qa/first-cycle-validation-2026-03-30.md", "w") as f: f.write(content)`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.

---
---

## STEP 3 — ANVIL QA ANALYST (findings quality assessment + final closeout)

---

> You are the Anvil QA Analyst. Before starting, read the Step 2 QA report at `anvil/knowledge/qa/first-cycle-validation-2026-03-30.md` and check its Output Receipt. If not Complete, stop and report. **Task:** Assess the quality and usefulness of Anvil's findings for the Planner. Read the cycle report at `anvil/knowledge/research/cycle-1-findings-2026-03-30.md`. Evaluate each finding section on 3 criteria: **(a) Actionability** — could the Planner write an executable plan based on this finding without needing a diagnostic first? **(b) Accuracy** — based on the cross-validation in Step 2, are these findings correct? **(c) Priority** — are the severity ratings (high/medium/low) reasonable? Produce a findings quality assessment with these sections: **(1) Coverage gaps quality** — are the top findings genuinely untested high-risk code, or are they false positives (e.g., simple getters that don't need tests)? Score: actionable / partly actionable / noise. **(2) Coupling hotspots quality** — are the flagged modules genuinely risky to modify, or are they just heavily-used utility functions (which is expected, not risky)? Distinguish between "high coupling because critical" and "high coupling because fragile." Score each. **(3) Clone candidates quality** — are the 1.0-similarity pairs worth consolidating, or are they test fixtures that are intentionally duplicated for isolation? Separate true production clones from acceptable test duplication. **(4) Staleness alerts quality** — are the stale chunks genuinely at risk, or is this just noise from dependency graph breadth? A function that imports a utility that changed isn't necessarily stale. Assess whether the staleness formula needs tuning. **(5) Overall assessment** — is the Cycle 1 output ready for the Planner to consume, or does it need tuning before it's useful? What's the signal-to-noise ratio? What thresholds should be adjusted for Cycle 2? **(6) Recommended Planner integration** — how should the Planner read Anvil findings? Recommend a specific protocol: which section of the cycle report to read during context loading, how to translate constraints into plan steps, when to ignore low-severity findings. Deposit the assessment to `anvil/knowledge/qa/findings-quality-assessment-2026-03-30.md` using `with open()`. **Final:** Update `anvil/PROJECT_STATUS.md` — add milestone: "Phase 6 First Cycle Validation complete — cross-validated against invoice-pulse specialist files, findings quality assessed, Planner integration protocol recommended. Anvil is operational." Update the "Next" section to reflect that Anvil is now ready for operational use. Move this plan to Done: `import shutil; shutil.move("/Users/marklehn/Desktop/GitHub/anvil/knowledge/decisions/executable-first-cycle-2026-03-30.md", "/Users/marklehn/Desktop/GitHub/anvil/knowledge/decisions/Done/executable-first-cycle-2026-03-30.md")`. Commit: `git -C /Users/marklehn/Desktop/GitHub/anvil add -A && git -C /Users/marklehn/Desktop/GitHub/anvil commit -m "chore: first cycle validation + findings quality assessment + move to Done"`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.
