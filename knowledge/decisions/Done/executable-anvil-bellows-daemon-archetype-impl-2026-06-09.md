# Anvil — Bellows daemon Archetype Implementation (BP3)
**Date:** 2026-06-09 | **Tier:** Medium | **Dispatch Mode:** bellows | **Test Scope:** full-suite | **Execution:** Step 1 (DEV) → Step 2 (QA) | **qa_steps:** 2 | **pause_for_verdict:** after_qa_step

## Context (Rule 27)
Implements the approved design `anvil/knowledge/architecture/bellows-daemon-archetype-design-2026-06-09.md` (BP3 diagnostic, in Done/). Plugs into the BP2 archetype registry (anvil main `f3d9193`). Purely additive: registers a second archetype (`daemon`) and flips bellows' SCAN_TARGETS entry from the BP2 placeholder `flask_service` to `daemon`. Does NOT touch `flask_service.py`, `classifier.py`, `scorer.py`, or any invoice-pulse-affecting logic. Regression: invoice-pulse classify-only hash must equal the BP2 baseline `59cc0d80781ef03d8b8633c1bf605f578e9143be2503a221b5d39e449ba33078` (classify-only, time-invariant — NOT a composite hash, per the 2026-06-09 LESSON); bellows must reach >=80% non-utility (design projects 84.5%, 131/155). Out of scope: bellows intent-layer (PROJECT_BRIEF/domain-glossary) — separate deferred follow-on.

---
---

## STEP 1 — Anvil Developer

---

> Before starting, read `anvil/knowledge/architecture/bellows-daemon-archetype-design-2026-06-09.md` and check the Output Receipt status; if not Complete, halt and report to the CEO. **Pre-edit verification (Rule 39):** re-run each (claim, query, expected_output) triple in the design's Section 6 Verification Blocks (V1-V6) before any edit; if any actual output differs materially from the recorded output, STOP — do not edit — and deposit a verification-mismatch report to `anvil/knowledge/flags/verification-mismatch-anvil-bellows-daemon-archetype-impl-2026-06-09-step-1.md` for Planner triage. **Then** read your specialist file and domain glossary. Implement the daemon archetype per design Sections 1-5 — reference those sections as your checklist; do not restate specs (Rule 11). **Work:** create `src/archetypes/daemon.py` mirroring `src/archetypes/flask_service.py`'s structure exactly, populated from the design — 13 roles (4 groups), 34 name rules, 12 file_path rules, empty decorator_rules, 8 scoring_weights profiles, 6 role_thresholds, 8 best_practices, 3 content_checks, 1 structural_check — ending with `register_archetype(daemon)`; add `import src.archetypes.daemon  # noqa: F401` to `src/archetypes/__init__.py`; in `src/config.py`, flip `SCAN_TARGETS["bellows"]["archetype"]` from `"flask_service"` to `"daemon"`. **Constraint:** additive only — do NOT modify `flask_service.py`, `classifier.py`, `scorer.py`, `db.py`, or anything that affects invoice-pulse classification. Add minimal new tests in `tests/test_daemon_archetype.py`: `get_archetype('daemon')` returns the definition; a sample daemon-pattern chunk classifies to its expected role; the registry holds both `flask_service` and `daemon`. Run `pytest tests/` to a pass/fail result and read the tail (do not infer green from a subset). **Scope (scope_check pre-empt):** modify ONLY `src/archetypes/daemon.py` (new), `src/archetypes/__init__.py`, `src/config.py`, and `tests/test_daemon_archetype.py` (new); if an edit requires any other file, halt and report rather than expanding scope. All file writes use `with open()`; no bash heredocs. Do NOT `git push`. Write the dev log, then commit (`git add` + `git commit`, no push). Standard prompt feedback → `anvil/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `anvil/knowledge/development/bellows-daemon-archetype-impl-2026-06-09.md`

---
---

## STEP 2 — Anvil QA Analyst

---

> **MANDATORY — Rule 20 self-check gate (do this, not just the table).** The `rule_20_self_check` gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` immediately followed by a line beginning `PASSED — SELF-CHECK PASSED`. The verification table does NOT satisfy this gate. After writing the report, run the canonical block and self-grep your own report for that banner before finishing — if it is absent the plan halts on re-dispatch. **Then:** Before starting, read `anvil/knowledge/development/bellows-daemon-archetype-impl-2026-06-09.md` and check the Output Receipt status; if not Complete, halt and report. Read your specialist file and domain glossary. **FIRST — Deliverable Verification (Rule 17).** Read the DEV receipt "Files Created or Modified (Code)" list; verify each exists with the described change (grep `daemon.py` for 13 roles + the name/file_path rule counts; grep `__init__.py` for the daemon import; grep `config.py` that bellows' archetype is `daemon`; `get_archetype('daemon')` returns the definition). Produce `| Deliverable | Expected | Status (✅/❌) | Evidence |`; any ❌ → fix or halt, do NOT close. **Checks (deposit raw output to evidence per Rule 18; never ✅ with hedging per Rule 19):** (1) **Invoice-pulse classify-identity (additive-regression gate)** — using the BP2 blueprint's regression harness (`archetype-flask-service-migration-blueprint-2026-06-09.md` Section 5) in classify-only mode against a FRESH COPY of the live anvil.db, compute the invoice-pulse `(file_path, name, functional_role)` hash and assert it equals the BP2 baseline `59cc0d80781ef03d8b8633c1bf605f578e9143be2503a221b5d39e449ba33078` (classify-only, time-invariant — NOT a composite hash) → `ip_classify_hash.txt`. (2) **Bellows classification** — classify bellows chunks under the daemon archetype against the copy; assert >=80% of the 155 classifiable chunks receive a non-utility role (design projects 84.5% / 131), every assigned role is in the daemon ROLES list, and the residual utility chunks are exclusively test helpers → `bellows_distribution.txt`. (3) **Full suite** — `pytest tests/`, read the tail, green (>= prior baseline + new daemon tests) → `pytest_full.txt`. (4) **Registry wiring** — `get_archetype('daemon')` returns the 13-role definition and both archetypes are registered → `registry.txt`. Then run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root with `plan_slug`: `anvil-bellows-daemon-archetype-impl-2026-06-09`, `qa_report_path`: `<absolute path to this QA report>`, `evidence_dir`: `<absolute path to the evidence dir>`, `required_evidence_files`: `[ip_classify_hash.txt, bellows_distribution.txt, pytest_full.txt, registry.txt, deliverable_grep.txt]`; include literal stdout; if FAILED, halt and report. **Final (Rule 8/23 order):** deliverable verification → QA deposit → update `PROJECT_STATUS.md` with a completed-milestone entry (BP3: daemon archetype shipped; bellows reclassified from 100% utility to ~84.5% non-utility; flask_service/invoice-pulse unchanged) → feedback append → final commit (`git add` + `git commit`, no push). **STOP — do NOT move this plan to Done;** the close path is owned by Bellows on the Planner's continue verdict. **Deposits:**
> - `anvil/knowledge/qa/2026-06-09-bellows-daemon-archetype-impl-qa.md`
> - `anvil/knowledge/qa/evidence/anvil-bellows-daemon-archetype-impl-2026-06-09/` (multiple files per Rule 20 self-check)
