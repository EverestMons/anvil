# Anvil — Bellows `daemon` Archetype Design (BP3, Diagnostic)
**Date:** 2026-06-09 | **Tier:** Medium | **Dispatch Mode:** bellows | **Execution:** Step 1 (SA) | **pause_for_verdict:** after_step_1

## Context (Rule 27)
This is the design pass blueprint `anvil/knowledge/architecture/anvil-extraction-contract-archetype-design-2026-06-08.md` section C.7 explicitly deferred ("These need to be authored after reading bellows' source — this diagnostic does not attempt that"). BP2 (now in `Done/`, commit `1115c2c`) shipped the archetype registry + `flask_service` head; the `daemon` slot is the second head. Closes the 2026-06-08 BACKLOG item "Mono-role `utility` classification for non-invoice-pulse projects" (all bellows chunks currently classify as `utility`). Additive — does not touch `flask_service` or invoice-pulse. This is a design diagnostic only; the implementing executable (DEV `archetypes/daemon.py` + register + SCAN_TARGETS entry; QA) is authored after CEO review of these findings.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Anvil Systems Analyst

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file and domain glossary first, then the blueprint `knowledge/architecture/anvil-extraction-contract-archetype-design-2026-06-08.md` section C.7 (preliminary daemon taxonomy) and the shipped `src/classifier_registry.py` + `src/archetypes/flask_service.py` (the `ArchetypeDefinition` shape you will mirror). **Post a 1-line "Read X." after each file read and a 1-line "Drafting Section N." at the start of each section** (keeps the inactivity timer warm). This is a design diagnostic — propose the bellows `daemon` archetype; author NO source, no `daemon.py`, no edits (the implementing executable comes after CEO review). **Primary data source is the Anvil DB, not raw bellows files:** query the live DB at the absolute path `/Users/marklehn/Developer/GitHub/anvil/anvil.db` (it is in the main repo, NOT the worktree) for bellows' already-ingested chunks — `code_chunks` filtered to the bellows `project_id` — to get the actual `file_path`, `name`, `chunk_type`, and sample `content` your classifier rules will key on. Confirm the current state: total bellows chunk count and that ~all carry `functional_role = 'utility'` (the 2026-06-08 BACKLOG mono-role finding; bellows cycle 2 is the valid baseline). If you need raw bellows source to confirm a pattern, read under the absolute path `/Users/marklehn/Developer/GitHub/bellows/`; if any such read is DENIED, halt and report — do NOT attempt a config or permission self-grant (see anvil BACKLOG 2026-06-09). **Section 1 — Role taxonomy.** Starting from C.7's preliminary 9 roles (`plan_dispatcher`, `worktree_manager`, `agent_lifecycle`, `config_loader`, `cache_manager`, `git_operator`, `template_engine`, `cli_command`, `utility`), refine against the actual bellows file/name population — confirm, rename, add, or drop roles so each maps to real bellows chunks; present as a table `| role | group | description | example bellows chunks (file:name) |`. **Section 2 — Classifier rules (priority-ordered).** Author `decorator_rules`, `name_rules`, `file_path_rules` as `(regex, role)` pairs in the same priority model `flask_service` uses (decorator → name → file_path → `utility` fallback); show each rule with the bellows chunks it matches, and report the projected role distribution (how many of the N bellows chunks each rule captures and the residual `utility` count). **Section 3 — Scoring profiles (defaults, untuned).** Propose `scoring_weights` per role and `role_thresholds`, mirroring sensible `flask_service`-style defaults adapted to daemon roles — flag explicitly as untuned starting defaults to revisit only if bellows becomes a recurring cycle target. **Section 4 — best_practices + content/structural checks.** Propose a minimal daemon-appropriate set (may be sparse or empty to start); do not invent practices without a bellows basis. **Section 5 — ArchetypeDefinition assembly + executable hand-off.** State exactly how these populate the `ArchetypeDefinition` fields (mirroring `flask_service.py`), the new `SCAN_TARGETS["bellows"]` entry shape (`{path, language: "python", archetype: "daemon"}`), and the additive-regression expectation for the downstream QA: invoice-pulse classification must stay byte-identical (flask_service untouched — use the classify-only hash, NOT a composite hash, per the 2026-06-09 LESSON on time-dependent volatility) and bellows must classify into the new roles, not all `utility`. Produce a Gap Assessment table `| Gap | Current State | Proposed State | Change Required |`. **Section 6 — `### Verification Blocks` (Rule 39)** with `(claim, query, expected_output)` triples for the load-bearing population facts you assert (bellows chunk count; current all-utility distribution) so the implementing executable can re-verify at edit time. Use `with open()` for the deposit; no heredocs. Standard prompt feedback → `knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `anvil/knowledge/architecture/bellows-daemon-archetype-design-2026-06-09.md`
