# Anvil — Archetype-ization + flask_service Migration (Build Plan 2)
**Date:** 2026-06-09 | **Tier:** Large | **Dispatch Mode:** bellows | **Test Scope:** full-suite | **Execution:** Step 1 (SA) → Step 2 (DEV) → Step 3 (QA) | **qa_steps:** 3 | **pause_for_verdict:** after_qa_step

## Context (Rule 27)
Derived from the SA design blueprint `anvil/knowledge/architecture/anvil-extraction-contract-archetype-design-2026-06-08.md` (commit a933b21) — section C (archetype classifier) + E.2 Step 3. Build Plan 1 (anvil main 35ee30f) already shipped the extraction contract, language-extractor registry, PythonExtractor encapsulation, and the chunk_type CHECK-constraint relaxation; `classifier.py` and `config.py` were left untouched (BP1 QA receipt `knowledge/qa/2026-06-08-extraction-contract-language-decoupling-qa.md`, scope guard check 6). BP2 implements only the archetype-consumption layer. **Regression gate:** invoice-pulse role_distribution + composite scores must be byte-identical before/after; the IP rules map 1:1 into the flask_service head. Out of scope (do not touch): the `resolve_dependencies` AST-residual (BACKLOG 2026-06-08, multi-language prereq); the bellows daemon archetype (BP3); `DEV_LOG_PATHS` folding.

---
---

## STEP 1 — Anvil Systems Analyst

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file and domain glossary first, then the blueprint `knowledge/architecture/anvil-extraction-contract-archetype-design-2026-06-08.md` (section C + E.2), then the source you need (`src/contracts.py`, `src/parsers/registry.py`, `src/classifier.py`, `src/config.py`, `src/db.py`, `src/cycle.py`, `src/scorer.py`, `src/detector.py`). **Post a 1-line "Read X." after each file read, and a 1-line "Drafting Section N." at the start of each section** (keeps the inactivity timer warm). This is a read + design + baseline-capture step — you modify NO source. **Section 1 — Re-grounded edit map.** The blueprint's line anchors predate BP1 and are stale; produce a Gap Assessment table with columns `| Gap | Current State (file:line) | Proposed State | Change Required |` pinning CURRENT anchors for: the `classify_chunk` signature + the DECORATOR/NAME/FILE_PATH rule chain in `classifier.py`; `SCAN_TARGETS`, `ROLE_SCORING_WEIGHTS`, `ROLE_THRESHOLDS` in `config.py`; `FUNCTIONAL_ROLE_SEEDS`, `BEST_PRACTICE_SEEDS`, their `init_db()` seeding calls, and the `functional_roles` table DDL in `db.py`; the classify + score call sites and the seeding path in `cycle.py`; the role-weight lookup (`ROLE_SCORING_WEIGHTS.get(role, ...)`) in `scorer.py`; the `CONTENT_CHECKS`/`STRUCTURAL_CHECKS` definitions in `detector.py` and where `find_best_practice_deviations` consumes them. **Section 2 — Consumer enumeration.** `grep -rn` every reader of `SCAN_TARGETS` across `src/` and `tests/`; list each hit with its exact access pattern (e.g. `SCAN_TARGETS[name]` vs `.get(name)`) that must change to the dict-form `["path"]`. **Section 3 — Changed-signature test surface (scope_check pre-empt).** `grep -rn` every caller across `src/` AND `tests/` of each signature that will change (`classify_chunk`, the scorer's `score`/`score_project` entry, the `cycle.py` seeding path, `find_best_practice_deviations`, the detector entry); enumerate the EXACT test files that will need fixture updates (the 2026-06-05 ripple caught `test_detector.py` only because callers were grepped — do the same). **Section 4 — flask_service 1:1 mapping.** Confirm the IP rules map cleanly into an `ArchetypeDefinition`: `FUNCTIONAL_ROLE_SEEDS`→roles, DECORATOR/NAME/FILE_PATH_RULES→rules in IDENTICAL priority order, `ROLE_SCORING_WEIGHTS`→scoring_weights, `ROLE_THRESHOLDS`→role_thresholds, `BEST_PRACTICE_SEEDS`→best_practices, `CONTENT_CHECKS`/`STRUCTURAL_CHECKS`→content_checks; FLAG any rule that does not map 1:1 (priority order is load-bearing — a reordering changes role assignment for multiply-matched chunks and breaks the regression gate). **Section 5 — Regression harness + baseline capture.** Design a reproducible classify+score replay for invoice-pulse that (a) operates on a COPY of the live main-repo DB at the absolute path `/Users/marklehn/Developer/GitHub/anvil/anvil.db` (the DB is NOT in the worktree), (b) must NOT write to any main-repo tracked path or otherwise dirty `main` (do not invoke the full cycle's cycle-report writer — replay classify+score against the copy's existing latest invoice-pulse cycle), and (c) emits a SHA-256 over invoice-pulse-scoped rows `(file_path, name, functional_role, composite_score)` under a deterministic sort. Embed the harness script verbatim in the deposit as a fenced block, run it against a copy with the CURRENT (pre-BP2) code, and record the baseline hash + row count in the deposit. **Section 6 — `### Verification Blocks` (Rule 39).** Emit `(claim, query, expected_output)` triples for: `classify_chunk` still uses module-global rules (not archetype-parameterised); `db.py` still seeds roles/practices inside `init_db()`; `SCAN_TARGETS` is still the plain-path dict; `config.py` still holds the weight/threshold dicts. **Deposits:**
> - `anvil/knowledge/architecture/archetype-flask-service-migration-blueprint-2026-06-09.md`

---
---

## STEP 2 — Anvil Developer

---

> Before starting, read `anvil/knowledge/architecture/archetype-flask-service-migration-blueprint-2026-06-09.md` and check the Output Receipt status; if not Complete, halt and report to the CEO before proceeding. **Pre-edit verification (Rule 39):** re-run each `(claim, query, expected_output)` triple from the blueprint's `### Verification Blocks` section before any edit; if any actual output differs materially from the recorded output, STOP — do not edit — and deposit a verification-mismatch report to `anvil/knowledge/flags/verification-mismatch-anvil-archetype-flask-service-migration-2026-06-09-step-2.md` for Planner triage. **Then** read your specialist file and domain glossary. Implement BP2 per blueprint section C and the SA Gap Assessment table — reference those sections as your checklist; do not restate specs (Rule 11). **Work:** create `src/classifier_registry.py` (`ArchetypeDefinition` + `ARCHETYPES` + `register_archetype`/`get_archetype`) per C.3; create `src/archetypes/__init__.py` and `src/archetypes/flask_service.py` migrating the IP rules 1:1 per C.6 and the SA mapping, **preserving rule priority order exactly**; refactor `SCAN_TARGETS` in `config.py` to the `{path, language, archetype}` dict form per C.5 and update every reader the SA enumerated to use `["path"]` — **keep `DEV_LOG_PATHS` as its own separate dict, do NOT fold it**; modify `classify_chunk` to take an `archetype` and consume archetype rules per C.3; modify the scorer to select weights via `archetype.scoring_weights.get(role, SCORING_WEIGHTS)` per C.3; modify `cycle.py` to load the project's archetype from `SCAN_TARGETS` and pass it to classify + score, and to seed `functional_roles` + `best_practices` per-cycle via idempotent `INSERT OR IGNORE` per C.8; modify `db.py` to remove the hardcoded `FUNCTIONAL_ROLE_SEEDS`/`BEST_PRACTICE_SEEDS` seeding from `init_db()` (it creates EMPTY tables) and add an `archetype TEXT` column to `functional_roles` via a PRAGMA-guarded idempotent migration applied to the live main-repo DB at `/Users/marklehn/Developer/GitHub/anvil/anvil.db` per C.8; move `CONTENT_CHECKS`/`STRUCTURAL_CHECKS` into `flask_service.content_checks` and have the detector/`find_best_practice_deviations` consume the archetype's checks per C.6. **Constraint:** behavior-preserving for invoice-pulse — the flask_service archetype must reproduce identical role assignments and composite scores; the SA harness is the gate. **Scope (scope_check pre-empt):** modify ONLY the files in the SA's Gap Assessment + changed-signature test list; the expected set is `src/classifier_registry.py`, `src/archetypes/__init__.py`, `src/archetypes/flask_service.py`, `src/config.py`, `src/classifier.py`, `src/scorer.py`, `src/cycle.py`, `src/db.py`, `src/detector.py`, a new `tests/test_classifier_registry.py` (or `tests/test_archetypes.py`), and the test files the SA enumerated (expected: `tests/test_classifier.py`, `tests/test_scorer.py`, `tests/test_cycle.py`, `tests/test_db.py`, `tests/test_detector.py`, `tests/test_best_practices.py`); if an edit requires a file NOT on the SA list, halt and report rather than silently expanding scope. Add the minimum new tests that distinguish correct from broken archetype behavior (registry returns flask_service; classify_chunk honors the archetype; init_db seeds nothing; a cycle seeds idempotently) — right-size, don't enumerate. Run `pytest tests/` to a pass/fail result and read the tail (do not infer green from a subset). All file writes use `with open()`; no bash heredocs. Do NOT `git push`. Write the dev log, then commit (`git add` + `git commit`, no push). Standard prompt feedback → `anvil/knowledge/research/agent-prompt-feedback.md`. **Deposits:**
> - `anvil/knowledge/development/archetype-flask-service-migration-2026-06-09.md`

---
---

## STEP 3 — Anvil QA Analyst

---

> **MANDATORY — Rule 20 self-check gate (do this, not just the table).** The `rule_20_self_check` gate greps your QA report for the byte-exact banner `Rule 20 — QA Self-Check Results` immediately followed by a line beginning `PASSED — SELF-CHECK PASSED`. The verification table does NOT satisfy this gate. After writing the report, run the canonical block and self-grep your own report for that banner before finishing — if it is absent the plan halts on re-dispatch. **Then:** Before starting, read `anvil/knowledge/development/archetype-flask-service-migration-2026-06-09.md` and check the Output Receipt status; if not Complete, halt and report to the CEO. Read your specialist file and domain glossary. **FIRST — Deliverable Verification (Rule 17).** Read the DEV receipt "Files Created or Modified (Code)" list; for every file verify it exists and contains the described change (grep the new modules; grep `classify_chunk` for the archetype parameter; grep `SCAN_TARGETS` for the dict form; `PRAGMA table_info(functional_roles)` against the live main-repo DB `/Users/marklehn/Developer/GitHub/anvil/anvil.db` for the `archetype` column). Produce `| Deliverable | Expected | Status (✅/❌) | Evidence |`; any ❌ → attempt fix, else halt and report — do NOT close. **Checks (deposit raw output to evidence per Rule 18; if a check can't complete mark it ❌ with a reason — never ✅ with hedging per Rule 19):** (1) **Byte-identical regression gate (load-bearing)** — copy the harness verbatim from the SA blueprint, run it against a FRESH COPY of the live anvil.db with the new code, record after-hash + row count, assert equal to the SA-recorded baseline hash + row count → `regression_hash.txt`. (2) **Full suite** — `pytest tests/`, read the tail, must be green (≥ the 240 baseline plus new archetype tests) → `pytest_full.txt`. (3) **Live DB schema (Rule 9)** — `PRAGMA table_info(functional_roles)` shows the `archetype` column; a fresh `init_db()` creates empty `functional_roles`/`best_practices`; a cycle seeds flask_service roles idempotently (re-run leaves counts stable) → `pragma_functional_roles.txt`, `seed_lifecycle.txt`. (4) **Archetype wiring** — `get_archetype('flask_service')` returns the IP role taxonomy and `classify_chunk` consumes it → `archetype_registry.txt`. Then run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root with `plan_slug`: `anvil-archetype-flask-service-migration-2026-06-09`, `qa_report_path`: `<absolute path to this QA report>`, `evidence_dir`: `<absolute path to the evidence dir>`, `required_evidence_files`: `[regression_hash.txt, pytest_full.txt, pragma_functional_roles.txt, seed_lifecycle.txt, archetype_registry.txt, deliverable_grep.txt]`; include literal stdout; if FAILED, halt and report. **Final (Rule 8/23 order):** deliverable verification → QA deposit → update `PROJECT_STATUS.md` with a completed-milestone entry summarizing BP2 (archetype registry + flask_service head, SCAN_TARGETS dict refactor, per-cycle seeding, byte-identical regression confirmed) → feedback append → final commit (`git add` + `git commit`, no push). **STOP — do NOT move this plan to Done;** the close path is owned by Bellows on the Planner's continue verdict. **Deposits:**
> - `anvil/knowledge/qa/2026-06-09-archetype-flask-service-migration-qa.md`
> - `anvil/knowledge/qa/evidence/anvil-archetype-flask-service-migration-2026-06-09/` (multiple files per Rule 20 self-check)
