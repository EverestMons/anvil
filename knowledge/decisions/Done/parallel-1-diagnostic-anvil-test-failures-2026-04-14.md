# Anvil — Diagnostic: Pre-existing Test Failures
**Date:** 2026-04-14 | **Tier:** Small | **Test Scope:** n/a (read-only) | **Execution:** Step 1 (DEV Investigation)
**Priority:** 2

## How to Run This Plan

This plan is picked up and executed by Bellows automatically.

## Context

Two tests have been failing in every Anvil pytest run since at least 2026-04-14 early morning:

- `tests/test_lab.py::test_find_cochange_patterns` — fails with `assert 0 >= 1` (empty `patterns` list)
- `tests/test_lab.py::test_find_staleness_alerts` — fails with `assert 'risky_func' in []` (empty `stale_names` list)

Both tests call finding functions against a `lab_project` fixture and expect non-empty results. Both get empty lists. DEV verified via git stash during Phase 2.1 refinement that the failures are pre-existing, NOT regressions from any recent plan. They have been carried forward as "expected pre-existing failures" for 4+ plans. This diagnostic investigates root cause so a subsequent executable can fix them permanently.

---
---

## STEP 1 — DEV (Investigation)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("anvil/knowledge/decisions/parallel-1-diagnostic-anvil-test-failures-2026-04-14.md", "anvil/knowledge/decisions/in-progress-parallel-1-diagnostic-anvil-test-failures-2026-04-14.md")`. Skip specialist file and glossary reads — pure code-tracing and fixture-inspection task. Working directory is `/Users/marklehn/Desktop/GitHub/anvil/`. Do NOT fix anything — investigate and report only. **Investigate the following and produce findings:** (1) Read `tests/test_lab.py` and quote the full definitions of `test_find_cochange_patterns` and `test_find_staleness_alerts` verbatim. Report line numbers. (2) Locate and quote the `lab_project` fixture definition (likely in `tests/test_lab.py` or `tests/conftest.py`). Report what tables/columns it seeds, what chunks it creates, and what dependencies or staleness signals it inserts. (3) Read `src/lab.py` and quote `find_cochange_patterns` verbatim. Report its SQL query (if any), its thresholds, and any filters it applies. Then quote `find_staleness_alerts` verbatim with same detail. (4) Run the two failing tests in isolation with verbose output: `python3 -m pytest tests/test_lab.py::test_find_cochange_patterns tests/test_lab.py::test_find_staleness_alerts -v -s` and capture the full output (including any print/log output from the finding functions if they have any). Write to `knowledge/research/anvil-test-failures-pytest.txt` via Python file I/O. (5) Inspect what the finding functions actually return against the fixture by adding temporary instrumentation via a Python one-liner: `python3 -c "import sys; sys.path.insert(0, '.'); from tests.test_lab import *; import sqlite3; from src.lab import find_cochange_patterns, find_staleness_alerts; conn = sqlite3.connect(':memory:'); [... use the actual fixture setup from test_lab.py ...]; print('cochange:', find_cochange_patterns(conn, pid)); print('staleness:', find_staleness_alerts(conn, pid, 1))"`. If that's too complex to inline, instead write a small investigation script at `/tmp/investigate_test_failures.py` that imports the fixture, runs the functions, and prints what each intermediate SQL query returns. Execute it. Report what the intermediate DB state looks like, what the SQL queries return, and where exactly the logic diverges from the test's expectation. (6) Categorize the root cause as one of: **(a) Fixture bug** — the `lab_project` fixture does not seed the data the finding functions need, so the functions return empty correctly given the input they're given. **(b) Logic bug** — the finding functions have a bug (bad SQL, wrong threshold, wrong column name) that causes them to miss data that IS in the fixture. **(c) Schema drift** — the functions query a column or table that no longer exists or has been renamed since the tests were written. **(d) Threshold misconfiguration** — the test expects a threshold relationship that the current code does not honor (e.g. test seeds staleness=0.7 expecting threshold=0.6, but code uses threshold=0.8). **(e) Something else** — describe it. (7) For each failing test, state specifically what would need to change to make it pass. If it's a fixture bug, state what data needs to be added to `lab_project`. If it's a logic bug, state which line in `src/lab.py` has the bug and what the fix would be. If it's schema drift, state which column needs renaming. Do NOT write the fix. (8) Check if the two failures share a root cause or are independent. Both functions deal with change/staleness signals which share upstream data — it's plausible they both fail because one fixture setup step is missing. State whether fixing one would likely fix the other. **Deposit findings to** `knowledge/research/anvil-test-failures-diagnostic-2026-04-14.md` using Python file I/O. Structure with sections matching items (1)-(8). Commit: `docs: diagnostic — pre-existing test failures (cochange, staleness)`. Then move the plan to Done: `import shutil; shutil.move("knowledge/decisions/in-progress-parallel-1-diagnostic-anvil-test-failures-2026-04-14.md", "knowledge/decisions/Done/parallel-1-diagnostic-anvil-test-failures-2026-04-14.md")`. Commit: `chore: move test-failures diagnostic to Done`. Standard prompt feedback protocol → `knowledge/research/agent-prompt-feedback.md`.
