# Anvil — Specialist Sync (Post-Phase 9)
**Date:** 2026-04-14 | **Tier:** Small | **Test Scope:** targeted | **Execution:** Step 1A (DOC) ∥ Step 1B (DOC) ∥ Step 1C (DOC) → Step 2 (QA)
**Priority:** 1

## How to Run This Plan

This plan is picked up and executed by Bellows automatically. Steps 1A, 1B, 1C are parallel — Bellows runs all three simultaneously.

---
---

## STEP 1A — DOC (ANVIL_DEVELOPER.md)

---

> **FIRST — claim this plan:** `import shutil; shutil.move("anvil/knowledge/decisions/executable-specialist-sync-2026-04-14.md", "anvil/knowledge/decisions/in-progress-executable-specialist-sync-2026-04-14.md")`. Skip specialist file reads — this IS the specialist update. Working directory is `/Users/marklehn/Desktop/GitHub/`. Read `anvil/agents/ANVIL_DEVELOPER.md`. Make the following surgical edits using Python `with open()` find-and-replace — do NOT rewrite the file: (1) Replace `**Version:** 1.0` with `**Version:** 2.0`; (2) Replace `**Last Updated:** 2026-03-29` with `**Last Updated:** 2026-04-14`; (3) Replace the Role Summary paragraph (starting "The Anvil Developer implements the SCAN → EXTRACT → SCORE → LAB pipeline") with: `The Anvil Developer implements the full SCAN → EXTRACT → CLASSIFY → PROVENANCE → SCORE → LAB pipeline for Anvil's structural codebase intelligence system. This specialist works exclusively within the Python + SQLite stack, building AST-based code parsing, functional role classification, dev log provenance ingestion, git history ingestion, MinHash similarity detection, best practice deviation detection, and health scoring — all following architectural blueprints from the Anvil Systems Analyst. The Developer maintains the test suite and ensures all pipeline stages produce correct, verifiable output. Phases 0–9 are complete; Anvil is operational against invoice-pulse.`; (4) Replace the Domain Focus paragraph with: `Python implementation of the SCAN → EXTRACT → CLASSIFY → PROVENANCE → SCORE → LAB pipeline. Python AST parsing for code chunk extraction (functions, classes, methods, modules, config blocks, test cases). Functional role classification via heuristic classifier (decorator > naming > file path rules, 25 roles across 5 groups). Dev log provenance ingestion from structured markdown files. SQLite operations (schema creation, CRUD, migrations). Git CLI integration for history ingestion. MinHash computation via datasketch for near-duplicate detection. Content hashing (SHA-256) for change detection between cycles. Best practice deviation detection via best_practices table (25 rules, per-role severity thresholds).`; (5) In Key Sources, add after the existing bullet points: `- Phase 7 classification blueprint (\`anvil/knowledge/architecture/phase7-classification-blueprint-2026-04-01.md\`) — functional role taxonomy and classifier implementation\n- Phase 8 best practices blueprint (\`anvil/knowledge/architecture/phase8-best-practices-blueprint-2026-04-01.md\`) — best_practices table and deviation scoring\n- Phase 9 research recommendations blueprint (\`anvil/knowledge/architecture/phase9-research-recs-blueprint-2026-04-01.md\`) — research recommendation finding type`; (6) Replace the Project-Specific Context paragraph (starting "Anvil is in Phase 0") with: `Anvil is operational. Phases 0–9 are complete. The full pipeline SCAN → EXTRACT → CLASSIFY → PROVENANCE → SCORE → LAB runs against invoice-pulse on demand via \`from src.cycle import run_cycle\`. Phase 1 targets invoice-pulse only — Python files parsed with stdlib \`ast\`. CEO decisions locked: Claude Code function execution model (no external APIs), run tests directly, separate DB from Forge. Current DB stats: 5,753 chunks, 312,733 symbol bindings, 26,966 health_score rows, 25 best_practice rules. Source files: scanner.py, extractor.py, classifier.py, provenance.py, scorer.py, lab.py, detector.py, cycle.py, db.py, config.py, parsers/python_parser.py.`; (7) Replace the Knowledge Base Index table (currently showing "none yet") with a table containing: `phase2-preflight-diagnostic-2026-04-14.md | 2026-04-14 | Pre-flight diagnostic for Phase 2 strategic audit — pipeline shape, schema tables, specialist staleness, readiness gaps`. Commit: `docs: sync ANVIL_DEVELOPER.md to Phase 9 reality`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed to Step 2. Wait for all parallel steps to complete and CEO confirmation.**

---
---

## STEP 1B — DOC (ANVIL_SYSTEMS_ANALYST.md)

---

> Skip specialist file reads — this IS the specialist update. Working directory is `/Users/marklehn/Desktop/GitHub/`. Read `anvil/agents/ANVIL_SYSTEMS_ANALYST.md`. Make the following surgical edits using Python `with open()` find-and-replace: (1) Replace `**Version:** 1.0` with `**Version:** 2.0`; (2) Replace `**Last Updated:** 2026-03-29` with `**Last Updated:** 2026-04-14`; (3) In the Role Summary, replace "SCAN → EXTRACT → SCORE → LAB" with "SCAN → EXTRACT → CLASSIFY → PROVENANCE → SCORE → LAB"; (4) In Domain Focus, replace "Pipeline architecture for the SCAN → EXTRACT → SCORE → LAB stages." with "Pipeline architecture for the SCAN → EXTRACT → CLASSIFY → PROVENANCE → SCORE → LAB stages. Schema design for Phase 7–9 additions: functional_roles table (25-role taxonomy), chunk_provenance table (dev log event storage), best_practices table (per-role pattern rules with severity thresholds)."; (5) In Key Sources, add after existing bullets: `- Phase 7 classification blueprint (\`anvil/knowledge/architecture/phase7-classification-blueprint-2026-04-01.md\`)\n- Phase 8 best practices blueprint (\`anvil/knowledge/architecture/phase8-best-practices-blueprint-2026-04-01.md\`)\n- Phase 9 research recommendations blueprint (\`anvil/knowledge/architecture/phase9-research-recs-blueprint-2026-04-01.md\`)`; (6) In Project-Specific Context, replace "Phase 1 targets invoice-pulse only, using stdlib AST. The pipeline is four stages: SCAN (content-hash change detection), EXTRACT (AST parsing + relationship extraction), SCORE (composite health scoring), LAB (actionable output generation)." with "Anvil is operational through Phase 9. The full pipeline is 6 stages: SCAN (content-hash change detection), EXTRACT (AST parsing + relationship extraction), CLASSIFY (functional role assignment — 25 roles across 5 groups), PROVENANCE (dev log ingestion), SCORE (composite health scoring with role-specific weights and thresholds), LAB (7 finding types including best_practice_deviations and research recommendations). Live schema: 12 data tables including best_practices, functional_roles, chunk_provenance added in Phases 7–8. Invoice-pulse current stats: 5,753 chunks, 312,733 symbol bindings, 25 best_practice rules."; (7) Replace Knowledge Base Index "none yet" row with: `phase2-preflight-diagnostic-2026-04-14.md | 2026-04-14 | Pre-flight diagnostic — pipeline shape, schema audit, Phase 2 readiness gaps`. Commit: `docs: sync ANVIL_SYSTEMS_ANALYST.md to Phase 9 reality`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed to Step 2. Wait for all parallel steps to complete and CEO confirmation.**

---
---

## STEP 1C — DOC (ANVIL_QA_ANALYST.md)

---

> Skip specialist file reads — this IS the specialist update. Working directory is `/Users/marklehn/Desktop/GitHub/`. Read `anvil/agents/ANVIL_QA_ANALYST.md`. Make the following surgical edits using Python `with open()` find-and-replace: (1) Replace `**Version:** 1.0` with `**Version:** 2.0`; (2) Replace `**Last Updated:** 2026-03-29` with `**Last Updated:** 2026-04-14`; (3) In Domain Focus, add after "health scores reflect actual code quality signals." the following sentence: "Best practice deviation verification — confirming that best_practice_deviations findings accurately reflect violations of per-role patterns in the best_practices table. Functional role classification verification — confirming that chunk functional_role assignments match heuristic rules for the top 5 roles."; (4) In Key Sources, add after existing bullets: `- Phase 7 classification blueprint (\`anvil/knowledge/architecture/phase7-classification-blueprint-2026-04-01.md\`)\n- Phase 8 best practices blueprint (\`anvil/knowledge/architecture/phase8-best-practices-blueprint-2026-04-01.md\`)\n- Phase 9 research recommendations blueprint (\`anvil/knowledge/architecture/phase9-research-recs-blueprint-2026-04-01.md\`)`; (5) In Project-Specific Context, replace "Phase 1 targets invoice-pulse, which has well-documented structural facts (68+ tables, 944+ tests, 11 validation gates, 27 engine modules) that serve as ground truth for cross-validation." with "Anvil is operational through Phase 9. Invoice-pulse ground truth facts: 68+ tables, 944+ tests, 11 validation gates, 27 engine modules, 5,753 chunks extracted, 312,733 symbol bindings, 25 best_practice rules across 5 functional role groups. Schema ground truth: 12 data tables — the 9 original tables plus functional_roles, chunk_provenance, best_practices (Phases 7–8 additions). All 12 tables must be verified via PRAGMA on every schema-touching QA step."; (6) Replace Knowledge Base Index "none yet" row with: `phase2-preflight-diagnostic-2026-04-14.md | 2026-04-14 | Pre-flight diagnostic — specialist staleness audit, Phase 2 readiness gaps`. Commit: `docs: sync ANVIL_QA_ANALYST.md to Phase 9 reality`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.
>
> **STOP. Do NOT proceed to Step 2. Wait for all parallel steps to complete and CEO confirmation.**

---
---

## STEP 2 — QA

---

> Before starting, verify all three Step 1 commits via `git --no-pager log --oneline -5`. Skip specialist file and glossary reads — mechanical verification only. **Deliverable verification:** For each of the three specialist files, verify: (1) `**Version:** 2.0` present, (2) `**Last Updated:** 2026-04-14` present, (3) `CLASSIFY` appears in the pipeline description, (4) Phase 7-9 blueprint references present in Key Sources, (5) Knowledge Base Index no longer shows "none yet". Produce verification table: `| File | Version | Date | CLASSIFY | Phase7-9 refs | Index updated | Status (✅/❌) |`. Write grep output evidence to `anvil/knowledge/qa/evidence/specialist-sync/verify.txt` via Python file I/O. Deposit QA report to `anvil/knowledge/qa/specialist-sync-qa-2026-04-14.md`. **Final:** Update `anvil/PROJECT_STATUS.md` — add entry: "2026-04-14: All three specialist files synced to Phase 9 reality (v2.0)." Move plan to Done: `import shutil; shutil.move("anvil/knowledge/decisions/in-progress-executable-specialist-sync-2026-04-14.md", "anvil/knowledge/decisions/Done/executable-specialist-sync-2026-04-14.md")`. Commit: `chore: QA report — specialist sync`. Standard prompt feedback protocol → `anvil/knowledge/research/agent-prompt-feedback.md`.
