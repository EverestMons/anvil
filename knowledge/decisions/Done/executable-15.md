# Anvil — BACKLOG → FORWARD Register Cutover (Reporting Phase 3, Stage 2 of 5)
**Date:** 2026-06-12 | **Tier:** Small | **Dispatch Mode:** bellows | **Execution:** Step 1 (DEV: registers) → Step 2 (DEV: trim) → Step 3 (QA) | **pause_for_verdict:** always | **qa_steps:** 3 | **Test Scope:** targeted

**Test Scope justification:** targeted — markdown register migration plus one file deletion; no production code, no tests.

## Context (Rule 27)
This executable implements diagnostic 13 — Stage 2 of the staged cutover (forge → anvil → invoice-pulse → bellows → shop_backlog), per the CEO fork verdicts recorded in processed-verdict-13-step-1: Option (a) archive-freeze + delete, staged per-project, governance edits separate, every-wrap reconciliation. Stage 1 (forge, plan 14) validated the template AND discharged the stands-alone gate's mechanical checks (all 14 plan ids assemble reconstruction data zero-exception; live-status correct — evidence at `forge/knowledge/qa/evidence/forge-backlog-cutover-2026-06-12/`); this stage does NOT re-run them. The human gate half (CEO review of FORWARD.md before trim) repeats per project via this plan's step-1 pause. The diagnostic findings at `/Users/marklehn/Developer/GitHub/governance/knowledge/research/reporting-phase-3-pre-cutover-unknowns-2026-06-12.md` are AUTHORITATIVE for the FORWARD.md format and migration rule (Section 4) and the anvil counts (Section 1: 176 lines, 8 truly-open, 7 closed-inline, single `## Open` section with closures annotated inline). ANVIL-SPECIFIC MIGRATION NUANCE: several anvil entries document a SHIPPED fix that carries an explicit open residual (markers like "Residual," "What is open," "Fix when picked up") — for those, the RESIDUAL is the truly-open item and migrates as the FORWARD entry (one line, pointing into the archive for context); entries whose fix shipped with NO open residual are closed-inline and do NOT migrate.

## How to Run
Bellows dispatches this plan automatically when deposited; no manual bootstrap required.

---
---

## STEP 1 — Anvil Developer (registers)

---

> **FIRST — before any reads or work: post a short visible chat message (1-2 sentences) confirming you are starting this plan and stating your immediate next action.** Liveness anchor — do NOT rename the plan file (Bellows owns the claim). **AFTER posting:** read your specialist file `agents/ANVIL_DEVELOPER.md`, then the diagnostic findings Sections 1 and 4 (path in Context), then `knowledge/BACKLOG.md` in full.
>
> **Part A — per-entry classification table (goes in your dev log, and is the QA conservation baseline):** for EVERY `###` entry in BACKLOG.md: entry date + short title | classification: `truly-open` / `closed-inline` / `shipped-with-open-residual` | one-line justification quoting the inline marker that decides it. Expected totals per diagnostic Section 1: 8 open-class (truly-open + residuals), 7 closed-inline — if your classification diverges from 8/7, do NOT force it; record the divergence with justification (the diagnostic's count was a survey; your per-entry table is the authoritative migration input).
>
> **Part B — create the two register files (BACKLOG.md untouched in this step):**
> - `knowledge/FORWARD.md` — diagnostic Section 4 format VERBATIM (title `# Anvil — Forward Register`, the standing-queue preamble blockquote, the 6-column table `| # | Added | Item | Type | Plan-id link | Status |`). One row per open-class entry from Part A, chronological order, numbered from 1. Item text: one line; for residuals, state the residual itself (not the shipped fix) and append `(context in BACKLOG-ARCHIVE.md)` where the verbose spec matters. Type: `deferred-work` unless the entry is explicitly a parked CEO decision → `ceo-decision-fork`. Plan-id link: `—` unless the entry names a deferred/halted plan id.
> - `knowledge/BACKLOG-ARCHIVE.md` — the full current BACKLOG.md content byte-identical, prepended with exactly: `# Anvil — BACKLOG Archive (frozen 2026-06-12)` + blank line + `> Read-only freeze of BACKLOG.md at the Reporting Phase 3 cutover (implements diagnostic 13, CEO Option (a)). The forward-looking queue now lives in knowledge/FORWARD.md; id-era closed history is generated reporting (forge reconstruction reports over bellows/lifecycle.db). Do not edit this file.` + blank line + `---` + blank line + original content unmodified.
>
> Write the dev log (Part A classification table + register-creation confirmations + `git status` showing BACKLOG.md untouched) to `anvil/knowledge/development/anvil-backlog-cutover-step1-dev-log-2026-06-12.md`. Use `with open()` for all file writes; no heredocs. **Deposits:**
> - `anvil/knowledge/FORWARD.md`
> - `anvil/knowledge/BACKLOG-ARCHIVE.md`
> - `anvil/knowledge/development/anvil-backlog-cutover-step1-dev-log-2026-06-12.md`

---
---

## STEP 2 — Anvil Developer (trim)

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. The CEO continue-verdict that resumed you IS the human gate review of FORWARD.md. This step does exactly one thing: `git rm knowledge/BACKLOG.md` and commit with message `chore: BACKLOG.md retired — FORWARD.md + BACKLOG-ARCHIVE.md per Phase 3 cutover (implements diagnostic 13) [<your plan id>]`. Confirm with `git status` (clean) and `ls knowledge/ | grep -i backlog` (only BACKLOG-ARCHIVE.md remains). Append a 3-line step-2 section (deletion commit SHA + both confirmations) to the step-1 dev log using `with open(..., "a")`. **Deposits:**
> - `anvil/knowledge/development/anvil-backlog-cutover-step1-dev-log-2026-06-12.md` (appended)

---
---

## STEP 3 — Anvil QA Analyst

---

> **FIRST — post a short visible chat message (1-2 sentences) confirming you are starting this step.** Do NOT rename the plan file. **AFTER posting:** read your specialist file `agents/ANVIL_QA_ANALYST.md`, the dev log, the diagnostic findings Sections 1 and 4, and both register files. **Verify, each with executed evidence (files into `knowledge/qa/evidence/anvil-backlog-cutover-2026-06-12/`):** (1) **Format conformance** — FORWARD.md matches the diagnostic Section 4 spec (title, preamble blockquote, exact 6-column header); output to `forward_format.txt`. (2) **Entry conservation** — independently re-classify every `###` entry of the archived BACKLOG (read it from BACKLOG-ARCHIVE.md body) into the same three classes as the dev log's Part A; your open-class count must equal the FORWARD.md row count, and every open-class entry must appear as a row (and no closed-inline entry leaked in); record your independent table and the comparison against both the dev log and the diagnostic's 8/7 survey; output to `entry_conservation.txt`. (3) **Archive integrity** — archive body below the frozen-header block is byte-identical to the deleted BACKLOG.md (`git show <pre-deletion-ref>:knowledge/BACKLOG.md` diffed against the extracted body — record exact commands); output to `archive_integrity.txt`. (4) **Trim landed** — `knowledge/BACKLOG.md` absent AND the deletion commit carries the `[<plan id>]` tag (`git log --oneline -5`); output to `trim_landed.txt`. If ANY check fails: fix in place only for trivial formatting; for classification disputes, halt and report to CEO.
>
> Run the canonical Rule 20 self-check from `RULE_20_SELF_CHECK_BLOCK.md` at the governance root. Use these values when filling in the template: `plan_slug`: `anvil-backlog-cutover-2026-06-12`; `qa_report_path`: `<your worktree absolute path>/knowledge/qa/anvil-backlog-cutover-qa-report-2026-06-12.md`; `evidence_dir`: `<your worktree absolute path>/knowledge/qa/evidence/anvil-backlog-cutover-2026-06-12/`; `required_evidence_files`: `[forward_format.txt, entry_conservation.txt, archive_integrity.txt, trim_landed.txt]`. Include the literal stdout output of the block in the QA report. If the block prints `FAILED`, halt and report to CEO instead of closing. Write the QA report with a verification table (item | method | evidence file | PASS/FAIL) and the Rule 20 banner block to `anvil/knowledge/qa/anvil-backlog-cutover-qa-report-2026-06-12.md`. Per Rule 8, also prepend a short dated entry to `PROJECT_STATUS.md` (worktree) noting the cutover (BACKLOG retired, FORWARD + ARCHIVE created, Stage 2 of 5). Use `with open()` for deposits; no heredocs. **Deposits:**
> - `anvil/knowledge/qa/anvil-backlog-cutover-qa-report-2026-06-12.md`
> - `anvil/knowledge/qa/evidence/anvil-backlog-cutover-2026-06-12/` (four evidence files per Rule 20 self-check)
